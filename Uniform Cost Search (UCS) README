START
Create a priority queue.
Insert the start node with cost 0.

WHILE priority queue is not empty DO
    Remove the node with minimum cost.
    IF node is goal THEN
        Display total cost.
        STOP.
    END IF

    FOR each neighbouring node DO
        Add neighbour with updated cost.
    END FOR
END WHILE

STOP
